export const getAllVehicles = async (): Promise<null> => {
  return null;
};

export const getVehicleById = async (): Promise<null> => {
  return null;
};

export const createVehicle = async (): Promise<null> => {
  return null;
};

export const updateVehicle = async (): Promise<null> => {
  return null;
};

export const deleteVehicle = async (): Promise<null> => {
  return null;
};