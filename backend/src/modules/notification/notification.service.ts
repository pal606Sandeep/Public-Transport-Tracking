export const getAllNotifications = async (): Promise<null> => {
  return null;
};

export const getNotificationById = async (): Promise<null> => {
  return null;
};

export const createNotification = async (): Promise<null> => {
  return null;
};

export const updateNotification = async (): Promise<null> => {
  return null;
};

export const deleteNotification = async (): Promise<null> => {
  return null;
};