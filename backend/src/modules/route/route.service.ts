export const getAllRoutes = async (): Promise<null> => {
  return null;
};

export const getRouteById = async (): Promise<null> => {
  return null;
};

export const createRoute = async (): Promise<null> => {
  return null;
};

export const updateRoute = async (): Promise<null> => {
  return null;
};

export const deleteRoute = async (): Promise<null> => {
  return null;
};