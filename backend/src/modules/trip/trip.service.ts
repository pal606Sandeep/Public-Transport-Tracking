export const getAllTrips = async (): Promise<null> => {
  return null;
};

export const getTripById = async (): Promise<null> => {
  return null;
};

export const createTrip = async (): Promise<null> => {
  return null;
};

export const updateTrip = async (): Promise<null> => {
  return null;
};

export const deleteTrip = async (): Promise<null> => {
  return null;
};