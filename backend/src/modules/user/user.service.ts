export const getAllUsers = async (): Promise<null> => {
  return null;
};

export const getUserById = async (): Promise<null> => {
  return null;
};

export const createUser = async (): Promise<null> => {
  return null;
};

export const updateUser = async (): Promise<null> => {
  return null;
};

export const deleteUser = async (): Promise<null> => {
  return null;
};