export const getAllDrivers = async (): Promise<null> => {
  return null;
};

export const getDriverById = async (): Promise<null> => {
  return null;
};

export const createDriver = async (): Promise<null> => {
  return null;
};

export const updateDriver = async (): Promise<null> => {
  return null;
};

export const deleteDriver = async (): Promise<null> => {
  return null;
};