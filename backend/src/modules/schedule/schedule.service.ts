export const getAllSchedules = async (): Promise<null> => {
  return null;
};

export const getScheduleById = async (): Promise<null> => {
  return null;
};

export const createSchedule = async (): Promise<null> => {
  return null;
};

export const updateSchedule = async (): Promise<null> => {
  return null;
};

export const deleteSchedule = async (): Promise<null> => {
  return null;
};