export const getAllStops = async (): Promise<null> => {
  return null;
};

export const getStopById = async (): Promise<null> => {
  return null;
};

export const createStop = async (): Promise<null> => {
  return null;
};

export const updateStop = async (): Promise<null> => {
  return null;
};

export const deleteStop = async (): Promise<null> => {
  return null;
};