export const registerUser = async (): Promise<null> => {
  return null;
};

export const loginUser = async (): Promise<null> => {
  return null;
};

export const refreshAccessToken = async (): Promise<null> => {
  return null;
};